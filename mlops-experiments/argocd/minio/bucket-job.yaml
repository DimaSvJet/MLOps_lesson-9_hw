apiVersion: batch/v1
kind: Job

metadata:
  name: create-mlflow-bucket

spec:
  template:
    spec:
      restartPolicy: Never

      containers:
        - name: bucket-init
          image: busybox

          command:
            - sh
            - -c
            - echo "Bucket creation placeholder"
