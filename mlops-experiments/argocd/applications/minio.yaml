apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: minio
  namespace: infra-tools

spec:
  project: default

  source:
    repoURL: https://github.com/DimaSvJet/MLOps_lesson-5-6.git
    targetRevision: master
    path: minio

  destination:
    server: https://kubernetes.default.svc
    namespace: mlflow

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
